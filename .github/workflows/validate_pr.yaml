name: Validate PR
on:
  push:
jobs:
  build:
    name: form-controls
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js 6.2.2
        uses: actions/setup-node@v1
        with:
          node-version: 6.2.2
      - run: yarn
      - run: yarn build
      - run: yarn test 
