name: Validate PR
on:
  push:
jobs:
  build:
    name: form-controls
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js 19.3.0
        uses: actions/setup-node@v1
        with:
          node-version: 19.3.0
      - run: yarn
      - run: yarn build
      - run: yarn test 
